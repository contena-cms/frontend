{# ludtwig-ignore-file #}
{# @internal #}

{% props
    page = null
%}

{% set attributeDefaults = {
    'data-page-id': page.id
} %}

<div class="ct-content-page" {{ attributes.defaults(attributeDefaults) }}>
    {% for element in page.elements %}
        {% sw_include '@Frontend/components/Ct/Content/_element.html.twig' with { element: element } only %}
    {% endfor %}
</div>
